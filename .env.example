# Example environment variables — copy to `.env` and fill values
PORT=3000

# Render provides a DATABASE_URL for its Postgres add-on. Example:
DATABASE_URL=postgres://username:password@host:5432/db_icd_codes

# Or set individual Postgres variables:
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=yourpassword
PGDATABASE=db_icd_codes
